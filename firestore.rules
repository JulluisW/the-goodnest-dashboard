rules_version = '2';

// service cloud.firestore {
//   match /databases/{database}/documents {
//     match /{document=**} {
//       allow read, write: if false;
//     }
//   }
// }

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read/write on all documents to anyone (NOT for production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}